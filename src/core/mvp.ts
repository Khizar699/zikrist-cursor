/** Phase 1: listen → lock [surah:ayah] → paint mushaf Arabic → follow.
 * Phase 2 translation is a static [surah:ayah] lookup after alignment is
 * flawless. Keep translation code; do not require it to start listening. */
export const MUSHAF_ONLY_MVP = true;
