# Fill: liturgy-ruku → ready

Read `_SHARED.md` first.

## Goal

WAV for `liturgy-ruku` locking `ruku_tasbih` (short form without وبحمده unless pack row says otherwise). No Quran locks.

## Success

Also bump `HANDOFF.md` in this same PR.

Mac `test:replay -- liturgy-ruku` PASS; `all` 14/14; this suite only.
